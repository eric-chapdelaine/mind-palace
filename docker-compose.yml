version: '3.3'
services:
  mind-palace:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./mind-palace.db:/app/mind-palace.db
    environment:
      - DATABASE_URL=sqlite:///./mind-palace.db
    restart: unless-stopped
